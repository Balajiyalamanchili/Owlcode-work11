class Solution:
    def digitCount(self, num: str) -> bool:
        l=[]
        for i in num:
            l.append(int(i))
        for i in range(len(l)):
            if l.count(i)!=l[i]:
                return False
        return True
